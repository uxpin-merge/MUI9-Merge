import * as React from 'react';
import IconButton from '../IconButton';
import Icon from '../../Icon/Icon';

export default (
  <IconButton uxpId="icon-button-1" color="primary">
    <Icon uxpId="icon-button-icon-1">favorite</Icon>
  </IconButton>
);
