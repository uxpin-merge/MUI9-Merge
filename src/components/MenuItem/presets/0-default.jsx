import * as React from 'react';
import MenuItem from '../MenuItem';

export default (
  <MenuItem uxpId="menu-item-1" value="temp-value-1">
    Change children prop to change text. Each MenuItem must have a unique Value Prop
  </MenuItem>
);
